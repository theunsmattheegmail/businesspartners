import Router from 'router';
import { ServiceConfig } from '../api';
/**
 * @param servicesConfig
 * @param version
 */
export declare function catalogService(servicesConfig: ServiceConfig[], version?: number): Router;
