import Router from 'router';
import { ServerConfig } from './api';
/**
 * @param inConfig
 */
export declare function createMockMiddleware(inConfig: ServerConfig, basePath?: string): Promise<Router>;
